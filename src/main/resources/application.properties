# ================ ?? application.properties (???) ================

# ?????? ?? ??
spring.application.name=hackaton-integrated
spring.profiles.active=dev

# ?? ??
server.port=${PORT:8080}
# server.servlet.context-path=/api  # ? ?? ?? ?? (????)
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.enabled=true
server.servlet.encoding.force=true
server.servlet.session.timeout=30m
server.servlet.session.cookie.max-age=1800

# ? MySQL local
#spring.datasource.url=jdbc:mysql://localhost:3306/studycoai
#spring.datasource.username=${DB_USERNAME:root}
#spring.datasource.password=${DB_PASSWORD:123456}
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

#???
spring.datasource.url=jdbc:mysql://google/hackathon_db?socketFactory=com.google.cloud.sql.mysql.SocketFactory&cloudSqlInstance=uenhaton:asia-northeast3:hackathon-mysql&useSSL=false
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

logging.level.com.zaxxer.hikari=DEBUG
logging.level.org.springframework.boot.autoconfigure.jdbc=DEBUG


# JPA/Hibernate ??
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.defer-datasource-initialization=true
spring.jpa.show-sql=true
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.jpa.properties.hibernate.types.print.banner=false
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# Spring Security ??
#spring.security.user.name=${ADMIN_USERNAME:admin}
#spring.security.user.password=${ADMIN_PASSWORD:admin}

# JSON ??? ??
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.deserialization.adjust-dates-to-context-time-zone=false

# ?? ?? ??
spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=true

# ?? ?? ??
server.error.include-message=always
server.error.include-binding-errors=always
server.error.include-stacktrace=on_param
server.error.include-exception=true

# OpenAI API ??
openai.api.key=${key_open_api}
openai.api.base-url=https://api.openai.com/v1

# AI ??? ??
app.use-real-ai=${USE_REAL_AI:true}

# Swagger/OpenAPI ??
springdoc.api-docs.path=/api/v3/api-docs
springdoc.swagger-ui.path=/api/swagger-ui.html
springdoc.swagger-ui.operations-sorter=method
springdoc.swagger-ui.display-request-duration=true
springdoc.swagger-ui.groups-order=DESC
springdoc.packages-to-scan=com.studycoAchl.hackaton.controller  # ? ??? ????
springdoc.show-actuator=false

# ?? ??? ??
chat.session.timeout=3600
chat.session.max-sessions-per-user=10
chat.message.max-length=2000
chat.message.batch-size=50
chat.websocket.allowed-origins=*

# ?? ?? ??? ??
problem.generation.max-problems-per-session=20
problem.generation.difficulty-levels=1,2,3,4,5
problem.generation.default-difficulty=3
problem.generation.timeout=600
problem.session.max-duration=1800
problem.session.auto-save-interval=300

# ?? ??
logging.level.com.studycoAchl.hackaton=DEBUG  # ? ??? ????
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.security.web=DEBUG

spring.h2.console.enabled=false
