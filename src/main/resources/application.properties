spring.application.name=hackaton

# ?? ??
server.port=8080
server.servlet.context-path=/api

# MySQL ?????? ?? (H2?? MySQL? ??)
spring.datasource.url=jdbc:mysql://localhost:3306/studyco_db?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=jj021204!@

# JPA ?? (MySQL??? ??)
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Security ??
spring.security.user.name=admin
spring.security.user.password=admin

# OpenAI API ??
openai.api.key=${OPENAI_API_KEY:test-key}

# ? ?? (??? ?? ??)
app.use-real-ai=false

# ?? ??
logging.level.com.studycoAchl=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG